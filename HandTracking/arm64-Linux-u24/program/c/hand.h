#include <stdio.h>
#include <stdlib.h>

struct Hands {
	
	struct Hand lefthand;
	struct Hand righthand;

};

struct Hand {

	float depth;		// automatically converted to the unit specified in the runner script
	

};
